# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment secrets
.env
.env.local
.env.production

# Anchor / Solana
blockchain/target
blockchain/.anchor
blockchain/test-ledger

# Bun lockfiles — we standardize on npm
bun.lock
bun.lockb

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code
.claude/
build_output.txt

# Lovable (historical)
.lovable/
.playwright-mcp/
audit-landing-mobile-360.png

# Internal pitch material — for filming the video, not for GitHub.
# Keep on local disk only; never push.
judges/pitch-deck-talk-only.html
judges/pitch-deck-talk-only.pdf

# Build artifacts (generated by `npm run sbom`; attached to releases instead)
sbom.cyclonedx.json
